{
  "name": "wellcall",
  "version": "0.0.0",
  "private": true,
  "workspaces": [],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
