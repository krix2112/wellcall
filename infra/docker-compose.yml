version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:v1.8.0
    container_name: wellcall-qdrant
    restart: always
    ports:
      - "6333:6333" # REST API mapped to localhost:6333
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
